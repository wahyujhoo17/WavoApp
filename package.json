{
  "name": "wavo-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w api",
    "dev:engine": "npm run dev -w engine",
    "build:engine": "npm run build -w engine"
  }
}
